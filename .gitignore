# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
storybook-static/

# Vite / tooling caches
.vite/
node_modules/.vite/
.cache/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage
coverage/

# Environment files
.env
.env.*

# Editor / OS
.DS_Store
.vscode/
.idea/

# Screenshot capture output from scry-sbcov (regenerated by --with-analysis)
screenshots/

# Produced by the demo itself. The runbook requires each attempt to start
# without the target implementation so the agent has to rediscover the
# component through Scry, so this must not be checked in.
src/components/AccountUpgradePanel.tsx
src/components/AccountUpgradePanel.stories.tsx

# Local deployer config. Untracked because it previously held a raw API key
# and was committed. Copy .storybook-deployer.example.json and fill it in, or
# pass --api-key / SCRY_API_KEY instead.
.storybook-deployer.json
